[project]
name = "legalos-research"
version = "0.1.0"
description = "AI Legal OS - Research Service"
requires-python = ">=3.12"
dependencies = [
    "legalos-common",
    "legalos-orchestrator",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "httpx>=0.27.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
]

[tool.uv.sources]
legalos-common = { workspace = true }
legalos-orchestrator = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
